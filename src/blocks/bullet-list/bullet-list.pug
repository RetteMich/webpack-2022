mixin bullet-list(options=[])
    -
        const {
            items = '',
            header ='',
        } = options
    .bullet-list
        h3.bullet-list__header=header
        ul.bullet-list__list
            each item in items
                li.bullet-list__item= item